# Frontend (Vite/React) environment variables
#
# In Render, set these in the Static Site -> Environment tab (they are injected at build time).

# REQUIRED in production deployments:
# Must point to your backend service's /api base.
VITE_API_BASE_URL=https://<your-backend-service>.onrender.com/api


