# Backend (Node/Express) environment variables
#
# For local dev, you can create a `.env` file (do not commit it).
# In Render, set these in the Service -> Environment tab.

# --- Required ---
# MongoDB connection string (MongoDB Atlas recommended)
CONNECTION_STRING=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# JWT signing secret (generate a long random string)
JWT_SECRET=replace_me_with_a_long_random_secret

# --- Required for AI replies (FastAPI service) ---
# Base URL ONLY (no /analyze) e.g. https://capstone-fastapi.onrender.com
FASTAPI_URL=https://<your-fastapi-service>.onrender.com

# Optional: request timeout for FastAPI calls
FASTAPI_TIMEOUT_MS=180000

# --- CORS (recommended for custom domains) ---
# Comma-separated list of allowed frontend origins.
# If you keep Render default domains, the backend also allows *.onrender.com automatically.
CLIENT_ORIGINS=https://<your-frontend-service>.onrender.com

# --- Optional ---
LOG_LEVEL=INFO
SESSION_SECRET=replace_me_with_a_long_random_secret

# Email (optional: used for verification emails)
EMAIL_USER=you@gmail.com
EMAIL_PASS=your_gmail_app_password

# Cloudinary (optional: only needed if you use Cloudinary upload flows)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=


